{
  "name": "@vendorapp/shared",
  "version": "0.0.1",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "lint": "echo \"(shared) lint later\"",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "echo \"(shared) build later\""
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
