export const PASSWORD_MIN_LENGTH = 8;
export const PASSWORD_LETTER_REGEX = /[A-Za-z]/;
export const PASSWORD_NUMBER_REGEX = /[0-9]/;

export const PASSWORD_MIN_LENGTH_MESSAGE = `password must be at least ${PASSWORD_MIN_LENGTH} characters`;
export const PASSWORD_LETTER_MESSAGE = 'password must include a letter';
export const PASSWORD_NUMBER_MESSAGE = 'password must include a number';
